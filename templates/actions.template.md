# Actions: {{PROJECT_NAME}}

> Generated by Specky v{{SPECKY_VERSION}}
> Date: {{GENERATED_DATE}}
> Verification Date: {{VERIFICATION_DATE}}

---

## Executive Summary

| Metric | Value |
|--------|-------|
| Total Tasks | {{TOTAL_TASKS}} |
| Files Affected | {{TOTAL_FILES}} |
| Estimated LLM Execution | {{ESTIMATED_TIME}} |
| Test Coverage Target | {{TEST_COVERAGE}}% |
| Single-Shot Confidence | {{CONFIDENCE_LEVEL}} |

## Target LLM Compatibility

This specification is executable by:
- [ ] Claude 4.5 Haiku (single-shot)
- [ ] Claude 4.5 Opus (single-shot)
- [ ] DeepSeek V3.2 (single-shot)
- [ ] Gemini 3.0 Flash (single-shot)
- [ ] Gemini 3.0 Pro (single-shot)
- [ ] GPT-4.5 (1-2 clarifications max)

---

## Prerequisites

### Environment
- Node.js {{NODE_VERSION}}+
- pnpm {{PNPM_VERSION}}+
- Git 2.40+

### Accounts Required
{{#each REQUIRED_ACCOUNTS}}
- [ ] {{this.name}} - {{this.purpose}}
{{/each}}

### API Keys
{{#each API_KEYS}}
- [ ] `{{this.env_var}}` - {{this.provider}}
{{/each}}

---

## Tech Stack Decisions

### Framework: {{FRAMEWORK_NAME}} {{FRAMEWORK_VERSION}}

**Query**: "{{FRAMEWORK_SEARCH_QUERY}} as of {{VERIFICATION_DATE}}"

**Options Evaluated**:
{{#each FRAMEWORK_OPTIONS}}
- {{this.name}} {{this.version}} - {{this.notes}}
{{/each}}

**Decision**: {{FRAMEWORK_NAME}} {{FRAMEWORK_VERSION}}
**Rationale**: {{FRAMEWORK_RATIONALE}}
**Verified**: {{VERIFICATION_DATE}} via Exa MCP

---

{{#each TECH_DECISIONS}}
### {{this.category}}: {{this.choice}}

**Query**: "{{this.search_query}} as of {{../VERIFICATION_DATE}}"

**Options Evaluated**:
{{#each this.options}}
- {{this.name}} - {{this.notes}}
{{/each}}

**Decision**: {{this.choice}}
**Rationale**: {{this.rationale}}
**Verified**: {{../VERIFICATION_DATE}}

{{/each}}

---

## Atomic Tasks

### Naming Convention
- `AT-XXX` = Atomic Task (touches ≤3 files)
- `IT-XXX` = Integration Task (connects multiple ATs)
- `QA-XXX` = Quality Assurance Task (testing only)

---

{{#each PHASES}}
## Phase {{this.number}}: {{this.name}}

> {{this.description}}
> Tasks: {{this.task_count}} | Files: {{this.file_count}}

{{#each this.tasks}}
### {{this.id}}: {{this.title}}

**Type**: {{this.type}}
**Estimated Time**: {{this.estimated_time}}
**Dependencies**: {{#if this.dependencies}}{{this.dependencies}}{{else}}None{{/if}}

**Files ({{this.files.length}})**:
{{#each this.files}}
{{@index}}. `{{this.path}}` - {{this.purpose}}
{{/each}}

**NOT Touched**:
{{#each this.excluded}}
- {{this}}
{{/each}}

**Acceptance Criteria**:
{{#each this.acceptance}}
- [ ] {{this}}
{{/each}}

**Tests** (`{{this.test_file}}`):
```typescript
{{this.test_code}}
```

---

{{/each}}
{{/each}}

## Quality Gates

### Before Each Task
- [ ] Read task spec completely
- [ ] Verify no file overlap with parallel tasks
- [ ] Check date-sensitive dependencies still current
- [ ] Ensure prerequisite tasks are complete

### After Each Task
- [ ] Run task-specific tests: `npm test -- {{task.test_pattern}}`
- [ ] Verify TypeScript compiles: `npm run typecheck`
- [ ] Visual check matches design spec (if UI task)
- [ ] Commit with descriptive message

### Before Phase Completion
- [ ] All tasks in phase marked complete
- [ ] Integration tests pass
- [ ] No TypeScript errors
- [ ] Bundle size within limits

---

## Success Metrics

| Metric | Target | Measurement |
|--------|--------|-------------|
| Test Coverage | {{TEST_COVERAGE}}% | `npm run coverage` |
| TypeScript Errors | 0 | `npm run typecheck` |
| Lighthouse Score | >90 | Chrome DevTools |
| Bundle Size (gzipped) | <{{BUNDLE_SIZE_LIMIT}}KB | `npm run build:analyze` |
| Build Time | <{{BUILD_TIME_LIMIT}}s | CI pipeline |

---

## Execution Instructions

### For Claude Code
```bash
# Load this spec
cat actions.md | claude -p "Execute this specification. Work through tasks sequentially. Ask for clarification only if acceptance criteria are ambiguous."
```

### For Cursor/Windsurf
1. Open project in IDE
2. Paste `actions.md` content into AI chat
3. Instruct: "Execute tasks AT-001 through AT-{{LAST_FOUNDATION_TASK}} sequentially"

### For Aider
```bash
aider --file actions.md --message "Implement tasks in order, committing after each"
```

### For Any LLM
1. Provide full `actions.md` content
2. Instruct: "This is a Specky specification. Execute each atomic task in order. Each task's acceptance criteria must pass before proceeding."

---

## Appendix A: File Tree (Final State)

```
{{PROJECT_NAME}}/
{{FILE_TREE}}
```

---

## Appendix B: Dependency Graph

```mermaid
{{DEPENDENCY_GRAPH}}
```

---

## Appendix C: Design Reference

{{#if DESIGN_REFERENCE}}
See attached Figma/design file: {{DESIGN_REFERENCE}}
{{else}}
Design tokens defined in `lib/design-tokens.ts`:
- Spacing: 4pt grid
- Typography: Inter/Geist variable
- Colors: OKLCH semantic palette
- Motion: 150ms/250ms/400ms easing
{{/if}}

---

**Specification Confidence**: {{CONFIDENCE_LEVEL}}
**Estimated Single-Shot Success Rate**: {{SUCCESS_RATE}}%
**Generated by**: Specky v{{SPECKY_VERSION}}
